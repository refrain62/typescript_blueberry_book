{
    const g = (num: number) => {
        for (let i = 0; i < num; i++) {
            console.log("Hello, world!");
        }
    };

    // Hello, world!
    // Hello, world!
    // Hello, world!
    g(3);
}