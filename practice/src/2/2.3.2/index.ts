{
    const width1 = 5;
    const width2 = 8;
    const height: number = 3;
    const area = (width1 + width2) * height / 2;
    
    // 19.5
    console.log(area);

    // 7.0ではなく7(number型では整数と少数の区別がない)
    console.log(3.5 * 2);
}
